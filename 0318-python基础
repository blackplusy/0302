1.元组
	元组和列表类似，小括号中包含元素，元素之间通过逗号进行分割
	(1,2,3)
	('simida','gaga')
	a.元组和列表的区别
		敏感信息传递给一个不了解函数，希望这个数据不被改动使用元组
		处理动态数据时候，经长更新数据，使用列表
		元组不可变，列表可变
		元组的速度较快

	b.创建元组
		如果元组中只有一个元素，需要在后面增加逗号消除歧义

	c.访问元组
		直接访问
		遍历访问
		成员访问
	d.删除元组
		del
	e.元组的切片和索引
	f.补充
		len()  元组中元素的个数(长度)
		max()  元组中的最大值
		min()  元组中的最小值
		max()、min()必须是同种类型数据比较
		元组.index()  元组中某个值第一次出现匹配的索引
		元组.count()  元组中某个元素出现的次数
2.文件的操作
	a.文件操作的应用场景
		1>网站记录日志的时候
		2>报表数据
		3>爬虫爬取的数据写入文件
		4>招聘网站的简历的导入导出
		5>自动化测试参数化
	b.文件操作实例
		1>读文件
		open()函数，2个参数，第一个是文件的路径，第二个是对文件进行操作
			打开文件--查看文件--关闭文件
		2>写入文件(覆盖原有内容)
		open()函数的第二个参数是w，代表写入，write只能写入字符串，如果没有该文件，自动创建
		3>追加
		追加内容open第二个参数a，注意换行
3.文件操作2
	a.read readline readlines区别
		read   		读取整个文件
		readline    读取下一行
		readlines   读取文件到一个迭代器中供遍历
	b.文件操作实例
		e:\\3.txt   内容
		名字     电话
		heygor  119
		o8ma    110
		判断110 是否是文件中存在的电话

	创建文件txl，内容为用户的名字和电话
	键盘输入用户名，判断是否存在于文件中，如果存在于文件中，直接输出对应电话，如果用户名在文件中没有找到，提示输入电话，电话输入完毕后，新的用户和新的电话写入到文件中


